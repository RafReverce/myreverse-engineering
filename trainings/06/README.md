# 06 - Decrypt password (1) (CrackMe)

**Цель:** Расшифровать пароль для доступа к программе.

## 🛠 Инструменты
- **Compiler:** MSVC (cl.exe)
- **Debugger:** x64dbg (x32 mode)
- **Decompiler:** Ghidra

## 🔍 Анализ и решение
1. **Поиск:** В Гидре нашел само условие ключа (0x55 или просто 55) и цикл While.
2. **Логика:** Из декомпилированного кода понял логику, что программа каждый цикл берет по байту (символ) и выполняет операцию XOR каждого байта с ключом 0x55».
3. **Значение(1):** Гидра уже отобразила мне нужные значения (0xXX) и пароль можно было расшифровать уже на этом моменте просто переведя его через простейший XOR калькулятор, но я пошел дальше .
4. **Значение(2):** Открыл тот же .exe файл, но уже в отладчике, нашел нужный адрес с помощью информации из Гидры, нашел проверку, поставил точку останова, запустил программу введя случайный пароль..
5. **Значение(3):** Программа дошла до точки (cmp), где был момент расшифровки валидного и его сверка. Через дамп памяти нашел адрес с зашифрованными байтами и расшифровал их, используя найденный ключ 0x55
6. **Расчет:** Через онлайн-калькулятор перевел байты в слово - получилось `Secret123`

## 📄 Файлы в папке
- `crack06.cpp` — Исходный код.
- `crack06.exe` — Оригинал.
- `Пароль: Secret123`


##Пояснения:
Программа использует симметричный XOR. Если Input ^ 0x55 == EncryptedByte, значит EncryptedByte ^ 0x55 == Input. Ключ статический — 0x55.

**Важно:** Eсли в дампе мусор/иероглифы — переключить вид на Hex (`Address -> Hex`). Искать данные в стеке через Ctrl+G (адрес `ebp-1c`)

**Важно:** Точка входа в логику проверки: `0x00FF1216`. Сравнение (CMP) происходит на `0x00FF121B`. Это помогло мне быстро найти цикл в x32dbg после анализа в Ghidra.